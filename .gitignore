
# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml

# SSH Keys — never commit private keys
*.pem

# Build artifacts
*.zip

# Assignment documents — not needed in repo
*.docx